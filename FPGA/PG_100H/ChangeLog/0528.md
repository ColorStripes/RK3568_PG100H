# 变更记录 — 2026-05-28（0528）

## 更新 0512_2 → 0528_0

**摘要：** 间隔两周后，一次大规模的卷积控制重写落地：`conv` / `conv11_16` 的控制与权重缓存
逻辑被重写（包括一个实验性的“无类型/带步长”权重变体），CDC 命令/数据通道也被改动。
**本次构建未完成** —— 布局布线无法收敛（0%），因此没有可用的资源汇总。

> **背景**：`conv_weight` 模块负责从 DMA 读取权重与偏置并缓存供给卷积模块——它使用 9 个独立
> FIFO 分别存储 3×3 卷积核的 9 个位置（1×1 模式只读中心位置），偏置单独存入 SPRAM，并通过
> `stride`（步长）与 `type`（卷积类型）参数区分不同权重排布。本日新增的
> `*_notype_stride` 变体即是在尝试改变权重按步长/类型的组织方式。

### 源码变更

**修改**
* `source/npu/conv/conv_conv.v`、`conv_ctrl.v`、`conv_in_ctrl.v`、`conv_mul_logic.v`、`conv_weight.v`
* `source/npu/conv11_16/conv11_conv.v`、`conv11_ctrl.v`、`conv11_in_ctrl.v`、`conv11_weight.v`
* `source/npu/npu_cdc_top.v`
* `source/npu/sppf/sppf_in_ctrl.v`
* `source/axi_crossbar/cdc_cmd_channel.v`、`cdc_data_channel.v`
* `source/constraints/cdc.fdc`

**新增**
* `source/npu/conv/conv_weight notype_stride.v`（实验性权重变体）
* `source/npu/conv11_16/conv11_weight notype_stride.v`
* `source/npu/conv/conv_mul_logic copy.v`（备份）
* `source/npu/sppf/sppf_in_ctrl111.v`（备份变体）

### 资源与时序

| 指标 | 0512_2 | 0528_0 |
|---|---|---|
| LUT | 36,441 | —（构建失败） |
| P&R 收敛率 | — | **0%** |

> `multiseed_summary.csv` 报告 `Used = NA` 且 `Pass Rate / Convergence Rate = 0.00%`，
> 即该快照的器件映射 / 布局布线阶段未能产生可用结果。
